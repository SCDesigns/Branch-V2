<div class="container form">
  <%= form_for @category do |f| %>
    <%= f.label :name %>:
    <%= f.text_field :name %><br>
    <%= f.collection_check_boxes :city_ids, City.all, :id, :name %><br>
    <%= f.submit %>
    <% end %>
</div>
