/* Prev / Next (Recent)
$(() => {
  $("#prev").on("click", function(e){
    $("div.recent").map( function(i = 0, l = branches.length, e) {
      console.log(this)
    })
    i += 5
    e.preventDefault();
  })
});


$(() => {
  $("#next").on("click", function(e){
    $("div.recent").map( function(i = 0, l = branches.length, e) {
      console.log(this)
    })
    i += 5
    e.preventDefault();
  })
});
*/
