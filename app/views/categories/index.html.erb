<div class="feed">
<% @categories.each do |category| %>
  <div class="container">
    <ul>
      <b><li><%= link_to category.name, category_path(category) %></li></b><br>
    </ul>
  </div>
<% end %>
</div>
